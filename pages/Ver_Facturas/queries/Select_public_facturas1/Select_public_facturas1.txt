SELECT
  *
FROM
  public.facturas
ORDER BY
  "{{facturas.sortOrder.column || 'id'}}" {{facturas.sortOrder.order !== "desc" ? "" : "DESC"}}
LIMIT
  {{facturas.pageSize}}
OFFSET
  {{facturas.pageOffset}}